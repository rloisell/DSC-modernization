import React from 'react'

export default function Activity() {
  return (
    <div>
      <h1>Activity</h1>
      <p>Port target: `WebContent/jsp/activity.jsp` — dynamic server-side logic will be moved into React + API calls.</p>
    </div>
  )
}
