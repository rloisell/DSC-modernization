---
work_package_id: "WP05"
title: "Activity.jsx — Templates tab (list, Use, Delete, Save as Template)"
lane: "planned"
subtasks:
  - "WP03"
  - "WP04"
phase: "Phase 4 — Activity Page Restructure"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
history:
  - timestamp: "2026-02-22T03:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated by GitHub Copilot spec-kitty session"
---

# Work Package Prompt: WP05 – Activity.jsx — Templates tab (list, Use, Delete, Save as Template)

## Context

Build the Templates tab UI — list, "Use" (pre-fill form), "Delete", "Save as Template".

## Files to Modify

- `src/DSC.WebClient/src/pages/Activity.jsx`
- `src/DSC.WebClient/src/hooks/useTemplates.js` (new)

## Steps

1. Create `useTemplates()` hook — `useQuery` on GET `/api/templates`;
   `useMutation` for POST and DELETE.
2. Templates tab content:
   - Table/list of user's templates
   - "Use" button: calls `setFormValues(template)`, switches to `new-entry` tab
   - "Delete" button: calls delete mutation, invalidates query
3. "Save as Template" button on New Entry form: opens a small modal / inline input
   for template name; POSTs to `/api/templates`.

## Acceptance

- [ ] Templates tab lists existing templates
- [ ] "Use" pre-fills form and switches tab
- [ ] "Delete" removes template and updates list
- [ ] "Save as Template" creates template and shows in list

